org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Mapbox secret downloads token — DO NOT commit a real value here.
# Set the real token in your user-level ~/.gradle/gradle.properties instead,
# or export MAPBOX_DOWNLOADS_TOKEN as an env var before building.
MAPBOX_DOWNLOADS_TOKEN=
